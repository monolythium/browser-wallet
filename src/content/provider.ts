// EIP-1193 in-page provider stub.
// Runs in MAIN world at document_start so dapps can detect it.
// TODO(monolythium-vision): attach the real EIP-1193 shim that proxies to the background service worker.
console.log("Monolythium Wallet: EIP-1193 provider will attach here");
